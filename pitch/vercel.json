{
  "buildCommand": "cd ../prototype && npm ci && VITE_PITCH_MODE=true npm run build && rm -rf ../pitch/dist && cp -r dist ../pitch/dist",
  "outputDirectory": "dist",
  "installCommand": "echo 'no install needed in pitch root'",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
